import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Picture | Photfolio",
  description: "About Page of Photfolio",
};

type Props = {};

const Picture = (props: Props) => {
  return <div>Picture</div>;
};

export default Picture;
